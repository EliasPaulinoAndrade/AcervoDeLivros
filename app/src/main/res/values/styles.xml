<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/secBlue</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="picker" parent="android:Widget.NumberPicker">
        <item name="android:drawSelectorOnTop">true</item>
        <item name="android:background">@color/firstGreen</item>
        <item name="android:colorBackground">@color/firstGreen</item>
        <item name="android:textColorPrimary">@color/firstGreen</item>
    </style>
</resources>
